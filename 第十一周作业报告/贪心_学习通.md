

# 贪心算法



#### 先交作业~

```c++
1. (简答题)
小组学习：

任选一题
第一题
理解背包问题和分糖果的解题思路，找出限制值，期望值，通过什么方法选择可以在满足限制的情况下对期望值贡献最大。在答题卡写出两题的解题思路。

第二题
写出钱币问题和区间问题解题思路，并用代码实现这两题
    
 背包问题：

限制值：装入的物品容量要小于等于背包容量

期望值：装入背包的总价值最大

方法：

如果装不下当前物品，那么前n个物品的最佳组合和前n-1个物品的最佳组合是一样的

如果装得下当前物品：

假设1：装当前物品，在给当前物品预留了相应空间的情况下，前n-1个物品的最佳组合是一样的

假设2：不装当前物品，那么前n个物品的最佳组合和前n-1个物品的最佳组合是一样的

选取假设1和假设2中较大的价值，为当前最佳组合的价值；

解题思路：

在解决问题之前，为描述方便，首先定义一些变量：Vi表示第 i 个物品的价值，Wi表示第 i 个物品的体积，定义V(i,j)：当前背包容量 j，前 i 个物品最佳组合对应的价值，同时背包问题抽象化（X1，X2，…，Xn，其中 Xi 取0或1，表示第 i 个物品选或不选）。

1、建立模型，即求max(V1X1+V2X2+…+VnXn)；

2、寻找约束条件，W1X1+W2X2+…+WnXn<capacity；

3、寻找递推关系式，面对当前商品有两种可能性：

包的容量比该商品体积小，装不下，此时的价值与前i-1个的价值是一样的，即V(i,j)=V(i-1,j)；

还有足够的容量可以装该商品，但装了也不一定达到当前最优价值，所以在装与不装之间选择最优的一个，即V(i,j)=max｛V(i-1,j)，V(i-1,j-w(i))+v(i)｝。


其中V(i-1,j)表示不装，V(i-1,j-w(i))+v(i) 表示装了第i个商品，背包容量减少w(i)，但价值增加了v(i)；

由此可以得出递推关系式：

j<w(i) V(i,j)=V(i-1,j)

j>=w(i) V(i,j)=max｛V(i-1,j)，V(i-1,j-w(i))+v(i)｝

如果要到达V(i,j)这一个状态有几种方式？

肯定是两种，第一种是第i件商品没有装进去，第二种是第i件商品装进去了。

4、填表，首先初始化边界条件，V(0,j)=V(i,0)=0；

5、表格填完，最优解即是V(number,capacity)=V(4,8)=10。

6、最优解回溯：


通过上面的方法可以求出背包问题的最优解，但还不知道这个最优解由哪些商品组成，故要根据最优解回溯找出解的组成，根据填表的原理可以有如下的寻解方式：

V(i,j)=V(i-1,j)时，说明没有选择第i 个商品，则回到V(i-1,j)；

V(i,j)=V(i-1,j-w(i))+v(i)时，说明装了第i个商品，该商品是最优解组成的一部分，随后我们得回到装该商品之前，即回到V(i-1,j-w(i))；

一直遍历到i＝0结束为止，所有解的组成都会找到。



分糖果：

限制值：m个糖果和糖果大小大于等于孩子对糖果的需求才能分给孩子

期望值：满足孩子的数量最多

方法：从需求最小的孩子开始发糖



解题思路：

1、先定义一下变量

    children    //饥饿度

    candies    //糖果

    child    //能吃饱的孩子数量

    candy    //candies指针

2、按饥饿度从小到大进行排序，糖果数量也是

3、用最小糖果对最小饥饿度孩子进行投喂，目的是使尽可能多的孩子满足

     如果孩子未全部满足同时糖果数未喂完，继续投喂，直至孩子全部满足或者糖果数量为0

    （投喂过程中如果糖果数小于孩子饥饿度，糖果数指针向后移一位；如果满足，糖果数指针向后移一位，

      同时能吃饱孩子数量加1）

```

